Delete a multipart-uploaded chunk from the database
---
parameters:
  - $ref: "#/parameters/upload_id"
responses:
  200:
    description: `upload_id` of deleted collection
    schema:
      type: object
      example: {'_key': 50841, '0': {}, '1': {}}

  404:
    description: Document with key of `upload_id` not found
    schema:
      type: number
      example: 50841

tags:
  - uploads
