{
  "name": "multinet",
  "version": "0.1.0",
  "description": "MultiNet API / web application",
  "scripts": {
    "lint:js": "yarn --cwd client run lint",
    "lint:py": "cd multinet && pipenv run lint",
    "start:server": "pipenv run flask-serve",
    "start:client": "cd client && yarn serve",
    "build:client": "cd client && yarn install",
    "build:docs": "pipenv run build-docs",
    "serve:docs": "pipenv run serve-docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/multinet-app/multinet.git"
  },
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/multinet-app/multinet/issues"
  },
  "homepage": "https://github.com/multinet-app/multinet#readme"
}
